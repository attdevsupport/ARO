#iMAP configuration file

# Carrier name
CARRIER=AT&T

# Device name
DEVICE=Samsung i997

#RRC state machine parameters
# DCH->FACH timer (sec)
DCH_FACH_TIMER=4

# FACH->IDLE timer (sec)
FACH_IDLE_TIMER=10

# Min IDLE->DCH promotion delay (sec)
IDLE_DCH_PROMO_MIN=1.5

# Avg IDLE->DCH promotion delay (sec)
IDLE_DCH_PROMO_AVG=2.0

# Max IDLE->DCH promotion delay (sec)
IDLE_DCH_PROMO_MAX=4.0

# Min IDLE->DCH promotion delay (sec)
FACH_DCH_PROMO_MIN=0.8

# Avg IDLE->DCH promotion delay (sec)
FACH_DCH_PROMO_AVG=1.5

# Max IDLE->DCH promotion delay (sec)
FACH_DCH_PROMO_MAX=3.0

# RLC threshold for uplink (bytes)
RLC_UL_TH=543

# RLC threshold for downlink (bytes)
RLC_DL_TH=475

# Threshold for resetting DCH timer (bytes)
DCH_TIMER_RESET_SIZE=320

# Timing window for resetting DCH timer (sec)
DCH_TIMER_RESET_WIN=0.3

# RLC consumption rate (^2) for uplink
RLC_UL_RATE_P2=0.0014

# RLC consumption rate (^1) for uplink
RLC_UL_RATE_P1=1.6

# RLC consumption rate (^0) for uplink
RLC_UL_RATE_P0=20.0

# RLC consumption rate (^2) for downlink
RLC_DL_RATE_P2=0

# RLC consumption rate (^1) for downlink
RLC_DL_RATE_P1=0.1

# RLC consumption rate (^0) for downlink
RLC_DL_RATE_P0=10

#Power
# DCH Power (w)
POWER_DCH=1.30

# FACH Power (w)
POWER_FACH=0.30

# IDLE Power (w)
POWER_IDLE=0

# Average power for IDLE->DCH promotion (w)
POWER_IDLE_DCH=0.65

# Average power for FACH->DCH promotion (w)
POWER_FACH_DCH=0.80

#Burst analysis
# Threshold for defining a burst (sec)
BURST_TH=1.5

# Threshold for defining a long burst (sec)
LONG_BURST_TH=5.0

# Threshold for user input window (sec)
USER_INPUT_TH=1.0

# Min cycle for periodical transfers (sec)
PERIOD_MIN_CYCLE=10.0

# Max tolerable variation for periodical transfers (sec)
PERIOD_CYCLE_TOL=1.0

# Min observed samples for periodical transfers
PERIOD_MIN_SAMPLES=3

# Threshold for duration of a large burst (sec)
LARGE_BURST_DURATION=5.0

# Threshold for size of a large burst (bytes)
LARGE_BURST_SIZE=100000

# Average power for active GPS
POWER_GPS_ACTIVE=0.45

# Average power for standby GPS
POWER_GPS_STANDBY=0.02

# Average power for camera on
POWER_CAMERA_ON=0.92

# Average power Wi-Fi connected (w)
POWER_WIFI_ACTIVE=1.16

# Average power Wi-Fi standby (w)
POWER_WIFI_STANDBY=0.02

# Average power for active Bluetooth (w)
POWER_BLUETOOTH_ACTIVE=0.576

# Average power for standby Bluetooth (w)
POWER_BLUETOOTH_STANDBY=0.02

# Average power when screen is on (w)
POWER_SCREEN_ON=0.637

#Time delta for throughput calculations (sec).
W_THROUGHPUT=0.5

